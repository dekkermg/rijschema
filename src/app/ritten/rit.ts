export interface IRit {
        ritId: number,
        ritNaam: string,
        ritCode: string,
        ritDatum: string,
        omschrijving: string,
        afstand: number,
        prijsPerKilometer: number
}