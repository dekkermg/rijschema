export interface IRit {
        ritId: 1,
        ritNaam: string,
        ritCode: string,
        ritDatum: string,
        omschrijving: string,
        afstand: number,
        prijsPerKilometer: number
}